<%= form_for [@post, @comment] do |f| -%>
  
   	<div class="field">
    		<%= f.label :comment, 'Comentario'  %>
    		<%= f.text_field :comment %>
    </div>

	<div class="action">
		  	<%= f.submit 'Enviar' %>
	</div>			

<%end%>

